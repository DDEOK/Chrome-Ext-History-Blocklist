# 변경 기록

압축해제로 로드해 쓰는 확장이라 **버전이 "지금 도는 게 어느 코드인가"를 알려주는 유일한 표시**다.
`src/` 나 `manifest.json` 을 고치면 반드시 버전을 올린다 (`tools/manifest.test.js` 가 강제).
팝업과 설정 화면 머리에도 로드된 버전이 뜬다.

## 1.4.0 — 2026-09-11

- **새로 들어온 도메인은 과거 기록을 전 기간 전수로 한 번 턴다.** 직접 등록했든 **다른
  기기에서 동기화로 들어왔든** 같다 — 동기화로 받은 기기에는 그 도메인 기록이 쌓여 있는데,
  그동안은 텍스트 검색으로만 훑어 Chrome 의 토큰화가 못 잡는 형태가 남았다
- 한 번 끝낸 도메인은 다시 훑지 않는다(`storage.local` 에 기기별로 기록). **마친 표시는
  끝난 뒤에** 남기므로 도중에 서비스 워커가 죽으면 다음 주기 스윕이 다시 시도한다
- "전체 훑어 정리" 버튼의 역할이 바뀌었다 — 평소에는 누를 일이 없고, 자동 스캔이 끊겼다고
  의심될 때 **전부 다시** 훑는 수단이다

## 1.3.0 — 2026-09-11

로직 점검에서 나온 5건을 고쳤다.

- **저장 구조를 도메인당 키 하나로 바꿨다** (`d:example.com` → 추가 시각).
  목록 전체를 배열 한 키에 담으면 `storage.sync` 가 키 단위로 마지막 쓰기만 남겨,
  **두 기기가 각각 추가하면 한쪽 추가분이 통째로 사라진다.** 키를 쪼개니 서버가 병합한다
- 옛 `domains` 배열은 첫 실행에 자동으로 옮긴다. **지우기 전에 그 기기의 로컬 저장소에
  사본**(`legacyDomainsBackup`)을 남기므로 되돌릴 수 있다. 동기화를 타지 않는 사본이다
- `sweep()` 이 겹쳐 돌던 것을 막았다 — 알람·설정변경·버튼이 동시에 불러 같은 URL 을
  두 번 세는 바람에 "지운 기록" 통계가 부풀었다
- **꺼진 상태에서 "전체 훑어 정리" 를 누르면 이제 지우지 않는다.** 끈 채로 버튼 하나에
  기록이 사라지는 것은 놀라운 동작이다
- `history.search` 가 `maxResults` 를 넘으면 말없이 잘라내므로, 상한을 치면 **기간을 반으로
  쪼개 다시 본다.** 기록이 몰린 구간에서 오래된 것부터 조용히 빠지던 것을 막는다
- 주기 스윕이 도메인 텍스트 검색에 더해 **최근 7일을 통째로** 훑는다. 검색 토큰화가
  못 잡는 형태를 회수한다
- 설정 화면 목록에 **추가 날짜**를 띄운다 (옮겨온 항목은 시점 기록이 없어 빈칸)

## 1.2.0 — 2026-09-11

- **확장 ID 고정** — `manifest.json` 에 `key` 를 넣어 설치 경로와 무관하게 ID 가
  `jhkhlikcigccmladmjnknhfijmjlnccc` 로 고정된다. **차단 목록이 기기 간에 동기화된다**
- ⚠️ **이 버전으로 올리면 기존 차단 목록이 사라진 것처럼 보인다.** ID 가 바뀌어 옛 ID 의
  `storage.sync` 통을 더는 안 읽기 때문이다. 도메인을 한 기기에서 다시 등록하면 나머지로 퍼진다
- `tools/extension_id.py` — `key` 에서 ID 를 계산한다
- 서명 개인키는 `keys/extension-key.pem` (저장소가 private 인 전제)

## 1.1.0 — 2026-09-11

- 주소표시줄 키워드 모드: `hb` + <kbd>Tab</kbd> → 도메인 + <kbd>Enter</kbd> 로 차단/해제
- 결과를 툴바 배지로 표시 (`＋` 차단 · `－` 해제 · `!` 실패)
- 팝업·설정 화면에 로드된 버전 표시

## 1.0.1 — 2026-09-11

> 이 번호는 **나중에 붙인 것**이다. 실제로는 버전을 안 올린 채 배포돼 카드에 계속
> `1.0.0` 이 떴고, 그래서 "고친 게 반영된 건지 모르겠다"가 됐다. 이 기록과
> `tools/manifest.test.js` 가 그 재발을 막는다.

- **서비스 워커가 등록되지 않던 문제 수정.** 존재하지 않는 `chrome.history.onTitleChanged`
  를 부르고 있어 확장이 통째로 뜨지 않았다 (`Service worker registration failed`)
- 그 이벤트가 맡던 재기록 회수는 삭제 직후 1.5초·6초 지연 재확인으로 대체
- `tools/api-surface.test.js` 추가 — 쓰는 `chrome.*` API 를 문서 확인 목록으로 고정

## 1.0.0 — 2026-09-11

- 첫 구현. 등록한 도메인과 서브도메인의 방문 기록을 방문 즉시 삭제
- 도메인 등록 시 과거 기록 자동 정리 · 5분 주기 스윕 · 설정 화면의 전체 훑기
- 팝업에서 현재 사이트 한 번에 차단/해제

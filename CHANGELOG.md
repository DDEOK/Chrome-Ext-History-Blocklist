# 변경 기록

압축해제로 로드해 쓰는 확장이라 **버전이 "지금 도는 게 어느 코드인가"를 알려주는 유일한 표시**다.
`src/` 나 `manifest.json` 을 고치면 반드시 버전을 올린다 (`tools/manifest.test.js` 가 강제).
팝업과 설정 화면 머리에도 로드된 버전이 뜬다.

## 1.2.0 — 2026-09-11

- **확장 ID 고정** — `manifest.json` 에 `key` 를 넣어 설치 경로와 무관하게 ID 가
  `jhkhlikcigccmladmjnknhfijmjlnccc` 로 고정된다. **차단 목록이 기기 간에 동기화된다**
- ⚠️ **이 버전으로 올리면 기존 차단 목록이 사라진 것처럼 보인다.** ID 가 바뀌어 옛 ID 의
  `storage.sync` 통을 더는 안 읽기 때문이다. 도메인을 한 기기에서 다시 등록하면 나머지로 퍼진다
- `tools/extension_id.py` — `key` 에서 ID 를 계산한다
- 서명 개인키는 `keys/extension-key.pem` (저장소가 private 인 전제)

## 1.1.0 — 2026-09-11

- 주소표시줄 키워드 모드: `hb` + <kbd>Tab</kbd> → 도메인 + <kbd>Enter</kbd> 로 차단/해제
- 결과를 툴바 배지로 표시 (`＋` 차단 · `－` 해제 · `!` 실패)
- 팝업·설정 화면에 로드된 버전 표시

## 1.0.1 — 2026-09-11

> 이 번호는 **나중에 붙인 것**이다. 실제로는 버전을 안 올린 채 배포돼 카드에 계속
> `1.0.0` 이 떴고, 그래서 "고친 게 반영된 건지 모르겠다"가 됐다. 이 기록과
> `tools/manifest.test.js` 가 그 재발을 막는다.

- **서비스 워커가 등록되지 않던 문제 수정.** 존재하지 않는 `chrome.history.onTitleChanged`
  를 부르고 있어 확장이 통째로 뜨지 않았다 (`Service worker registration failed`)
- 그 이벤트가 맡던 재기록 회수는 삭제 직후 1.5초·6초 지연 재확인으로 대체
- `tools/api-surface.test.js` 추가 — 쓰는 `chrome.*` API 를 문서 확인 목록으로 고정

## 1.0.0 — 2026-09-11

- 첫 구현. 등록한 도메인과 서브도메인의 방문 기록을 방문 즉시 삭제
- 도메인 등록 시 과거 기록 자동 정리 · 5분 주기 스윕 · 설정 화면의 전체 훑기
- 팝업에서 현재 사이트 한 번에 차단/해제
